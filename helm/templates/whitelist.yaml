apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  labels:
    app.kubernetes.io/instance: {{ .Chart.Name }}
  name: vp-direct-ipwhitelist
spec:
  ipWhiteList: {{ .Values.ipWhiteList_direct }}

---

apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  labels:
    app.kubernetes.io/instance: {{ .Chart.Name }}
  name: vp-liveness-ipwhitelist
spec:
  ipWhiteList: {{ .Values.ipWhiteList_liveness }}

---

apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  labels:
    app.kubernetes.io/instance: {{ .Chart.Name }}
  name: vp-http-ipwhitelist
spec:
  ipWhiteList: {{ .Values.ipWhiteList_http }}