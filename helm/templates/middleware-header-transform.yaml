

apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
    name: middleware-header-rename
spec:    
  plugin:
    htransformation:
      Rules:
        - Header: X-Forwarded-Tls-Client-Cert
          Type: Rename
          Value: x-vp-auth-cert